const express = require('express');
const router = express.Router();

// Placeholder for trips routes
router.get('/', (req, res) => {
  res.json({ message: 'Trips feature coming soon' });
});

module.exports = router;