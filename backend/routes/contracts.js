const express = require('express');
const router = express.Router();

// Placeholder for contracts routes
router.get('/', (req, res) => {
  res.json({ message: 'Contracts feature coming soon' });
});

module.exports = router;